# Provenance ledger

Every file that contains code, tables or constants derived from another
project is recorded here, per the rules in [../LEGAL.md](../LEGAL.md) §2. A
port that is not listed here is a bug.

| File | Upstream project | Upstream file & revision | License | What was taken |
|---|---|---|---|---|
| `crates/hz-io/src/project.rs` | EBU ADM Renderer (`ear`) 2.2.0 | `ear/fileio/adm/data/2094_common_definitions.xml` | BSD-3-Clause | The nominal azimuth and elevation of each BS.2051 speaker label in the `SPEAKERS` table. These are values published in ITU-R BS.2094; they were read from that copy rather than recalled, because a `DirectSpeakers` block whose label and position disagree is one a renderer may resolve either way. |
| `crates/hz-io/src/project.rs` | `truehd` 0.7.1 (Rainbaby) | `src/structs/oamd.rs`, `ObjectAudioMetadataPayload::get_damf_pos` | Apache-2.0 | No code. The **derivation** of the master format's coordinate senses — that the y axis is sign-flipped out of the payload, so +1 is front — which is what makes the projection onto ADM the identity rather than a guess. Recorded here because getting it wrong is invisible and the source deserves the credit. |

| `crates/hz-analysis/src/kweighting.rs` | FFmpeg | `libavfilter/f_ebur128.c` | LGPL-2.1-or-later | The parametrisation of the BS.1770 K-weighting pair — the shelf's centre frequency, gain and Q, and the high-pass's centre frequency and Q — which FFmpeg recovered by working backwards from the coefficients the recommendation publishes at 48 kHz. Taken because it is what makes the filter designable at 96 and 192 kHz instead of tabulated at one rate; the test asserts it reproduces the published numbers. |

| `crates/hz-core/src/coords.rs` | EBU ADM Renderer (`ear`) 2.2.0 | `ear/core/objectbased/conversion.py`, `ear/core/geom.py`, `ear/common.py` | BSD-3-Clause | The point conversions of ITU-R BS.2127-0 §10 — the five-corner horizontal map, the tangent azimuth law and the elevation warp — ported to Rust and checked numerically against the original: zero difference over 1680 positions. Taken rather than reconstructed because a mapping this shape is not something to trust oneself to have remembered. |

| `crates/hz-mlp/src/crc.rs` | FFmpeg N-122487-g43dbc011fa | `libavcodec/mlp.c`, `libavutil/crc.c` | LGPL-2.1-or-later | The four integrity fields of an MLP stream: the polynomials and initial values of the three checksums, the rule that each XORs its last element in rather than feeding it through the table, and the restart header's bit-range checksum with its two excluded leading bits. Taken because none of it is derivable and all of it is exactly checkable — the two checksums that guard a whole access unit are asserted against streams FFmpeg wrote, and one of them was wrong until they said so. |

| `crates/hz-mlp/src/frame.rs` | FFmpeg N-122487-g43dbc011fa | `libavcodec/mlpenc.c` (Ramiro Polla, Jai Luthra), `libavcodec/mlp.h` (Ian Caulfield) | LGPL-2.1-or-later | The bitstream layout of an access unit: field order and widths of the major sync, the substream header, the restart header, the decoding parameters and the block data, and the arithmetic of the access unit's length and parity nibble. The structure is the format's and could have come from the specification; it came from here, so it is recorded here. The analysis half of `mlpenc.c` — filters, matrices, codebook search — is **not** ported, and nothing in this project is a transliteration of its encoding decisions. |

| `crates/hz-mlp/src/filter.rs` | FFmpeg N-122487-g43dbc011fa | `libavcodec/mlpdsp.c`, `mlp_filter_channel` | LGPL-2.1-or-later | The decoder's filter arithmetic, which an encoder has to invert exactly: the 64-bit accumulator, the arithmetic shift by the FIR filter's own shift, and which of the two state buffers receives the reconstructed sample and which the residual. Read from the decoder rather than the encoder, because what a lossless encoder must match is what the decoder will do. The coefficient search, the order decision and the context length are ours and are not FFmpeg's. The second filter's arithmetic — that its state holds past residuals rather than past samples, and that both filters share the first one's shift — comes from the same function. |

| `crates/hz-render/src/panner.rs` | — | — | — | **Nothing.** Vector-base amplitude panning is written from the published method — Pulkki, JAES 45(6), 1997, and the multiple-direction extension of 1999 — over a brute-force convex hull; no implementation of it was read or ported. Listed because a panner existed next door and taking it would have been the obvious thing. |

| `crates/hz-mlp/src/lpc.rs` | — | — | — | **Nothing.** Levinson-Durbin and the Welch window are textbook, and the quantiser's decisions — how the shift is chosen, the error carried between coefficients — were made here. Listed only to record that FFmpeg's `libavcodec/lpc.c` was *not* used, since it would have been the obvious thing to take. |

| `crates/hz-mlp/src/huffman.rs` | FFmpeg N-122487-g43dbc011fa | `libavcodec/mlp.c`, `ff_mlp_huffman_tables`; `libavcodec/mlpdec.c`, `calculate_sign_huff` | LGPL-2.1-or-later | The three codebooks — every code and its length — and the derivation of the offset a decoder adds after reassembling a value, including the conditional that leaves one of its terms out when a derived shift goes negative. The tables are the format's and could only be transcribed; the test checks that what was transcribed is a prefix code satisfying Kraft, which a mistyped digit would not be. The search that chooses between them is ours. |

| `crates/hz-mlp/src/matrix.rs` | FFmpeg N-122487-g43dbc011fa | `libavcodec/mlpdsp.c`, `ff_mlp_rematrix_channel`; `libavcodec/mlpdec.c`, `read_matrix_params` | LGPL-2.1-or-later | The decoder's matrix arithmetic, which an encoder has to invert exactly — the 64-bit accumulator over every source, the shift by fourteen, and the fact that the destination's own coefficient makes the operation an addition the encoder can subtract — and the wire format of a matrix, including that coefficients are written for two channels the stream does not carry. The choice of weight, of direction, and of when to use a matrix at all are ours and differ from FFmpeg's, which uses a ratio of channel ranges where this uses least squares. |

| `crates/hz-mlp/src/format.rs`, `frame.rs`, `reader.rs` | `truehd` 0.7.1 (Rainbaby) | `src/structs/restart_header.rs`, `src/structs/channel.rs` (`ExtraChannelMeaning`), `src/structs/matrix.rs`, `src/process/mod.rs` (`PresentationMap`), `src/structs/access_unit.rs` | Apache-2.0 | The immersive half of the format, which the reference stream shows exists but does not explain. Four things were taken: that the restart sync word is a three-valued field (`0x31EA` / `0x31EB` / `0x31EC`) with a rule about which substream may carry which, rather than thirteen bits of sync and a noise-type flag; that the three words select three different matrix syntaxes, and which; the arithmetic by which `substream_info` and `extended_substream_info` become one substream mask per presentation, ported into `Coded::presentation_channels`; and the field layout of the extra channel meaning's sixteen-element block; and the substream directory's second word — nine signed bits of gain as a power of two in sixty-fourths, three of how long it stands, the rule that a decoder counts the unit carrying the next word before reading it, the relation the major sync's start-up gain has to obey; and the extra data block — that the major sync's flags bit 12 and not the block itself says whether it holds an Evolution frame, the two parity folds and the constant one of them starts from, and the syntax of an Evolution frame and its payload configuration, including the group coding a payload's length uses. Taken because none of it is derivable and the alternative is guessing at a header that parses either way — and every one of them is checked against the reference stream, which is a thing neither project wrote. The C matrix syntax's field order and widths are ported too, along with the scaling that makes it arithmetically the other two's — `raw << (18 + shift - frac_bits)` against an accumulator shifted down by eighteen. What is **not** taken: the interpolating second coefficient set is written clear rather than used, and the decisions about what an immersive stream should contain, and when to spend a matrix on one, are ours. |

| `crates/hz-mlp/src/encoder.rs` | `truehd` 0.7.1 (Rainbaby) | `src/process/decode.rs`, `DecoderState::reset_decoder_substream_state` and the lossless-check accumulation | Apache-2.0 | No code. Two **behaviours** an encoder has to match and cannot see from the outside: that a restart header clears the decoder's filter state, which is what makes a restart a point the stream can be joined at and which FFmpeg's decoder does not do; and that the lossless check shifts each channel's contribution by `channel & 7`, so the shift wraps past eight channels. The first was found by running both decoders rather than by reading, and confirmed here; the second is a one-line fact that is invisible below sixteen channels and wrong above them. |

| `crates/hz-mlp/tests/decoder/mod.rs` | FFmpeg N-122487-g43dbc011fa | `libavcodec/mlpdec.c` (Ramiro Polla, Ian Caulfield) | LGPL-2.1-or-later | The decoder's *shape*, which the reference decoder in the test tree follows: the block loop and its terminating bit, the order the decoding parameters are read in and which presence flag guards each, the order a channel's parameters come in, and that a substream is filtered, then rematrixed over the whole access unit, then shifted and emitted. The arithmetic itself is the same as the `filter`, `matrix` and `huffman` rows above and is checked against them from the other direction; what is taken here is the sequence, which is not derivable from a stream. This file is test-only and nothing shipped links it. |

| `crates/hz-meta/src/oamd.rs` | `truehd` 0.7.1 (Rainbaby) | `src/structs/oamd.rs` | Apache-2.0 | The object audio metadata payload's grammar: the field order and widths of the payload header, the program assignment, the object element and its update blocks, and every field of an element's basic and render information — including the two carried memories that make "unchanged" mean what it means, the gain index's two-branch mapping, and the sixteen ramp durations the four-bit index names together with the two the two-bit code does. Taken because none of it is derivable and all of it is exactly checkable: the reference stream's own payload reads through this and writes back byte for byte, which a transcription error would not survive. The model above it — what an immersive programme *is*, and the decision to refuse a shape rather than approximate it — is ours, as is the writer's choice of when to say "unchanged". |

| `xtask/Cargo.toml` | `truehd` 0.7.1 (Rainbaby) | the crate itself | Apache-2.0 | Not a port: a **dependency**, in the development harness only, used as an oracle. It reads back every object metadata payload the encoder writes, in process, so that the check does not depend on which build of the `truehdd` binary happens to be installed — which turned out to matter. Nothing shipped depends on it. |

| `crates/hz-analysis/src/bands.rs` | — | — | — | **No code.** The ERB-rate scale and the equivalent rectangular bandwidth of the auditory filter — `21.4·log₁₀(4.37·F + 1)` and `24.7·(4.37·F + 1)`, `F` in kilohertz — are the published formulas of B. R. Glasberg and B. C. J. Moore, "Derivation of auditory filter shapes from notched-noise data", *Hearing Research* 47, 1990. Written from the paper; the split of a block's spectrum into equal steps of that scale, and the choice to make it a transform rather than a filterbank, are ours. |

| `crates/hz-io/src/master/metadata.rs` | `damf` crate (harletty-bridge) | `damf/src/damf.rs`, `Zones::from_u8`; `truehd` 0.7.0 (Rainbaby), `src/structs/oamd.rs`, the render-info decode | Apache-2.0 | Two small tables, read the other way round. The order in which the object metadata payload's three-bit zone constraint names the master's zones — none, no back, no sides, centre and back, screen only, surround only — from the bridge's master writer, which reads the code back into the name; and the quantisation of the screen and depth factors — an eighth above nought in three bits, a quarter in two — from the reference decoder. Taken because a fold that writes an element's rendering has to write the code the master's word stands for, and neither is derivable. |

| `crates/hz-analysis/src/partial.rs` | — | — | — | **No code.** The compressive law of specific loudness, `(E + A)^α − A^α`, with its exponent of a fifth and its threshold term, is the model of B. C. J. Moore, B. R. Glasberg and T. Baer, "A model for the prediction of thresholds, loudness, and partial loudness", *J. Audio Eng. Soc.* 45(4), 1997, as ISO 532-2:2017 standardises it; the constants written are the ones the model states for the flat region of the middle ear, and the tables it gives for the rest are not carried. The definition of what a sound *adds* — the loudness with it in, less the loudness without it — is ours, as is the spatial weighting of the masker in `hz-cluster`, and neither is the paper's partial-loudness formula. The calibration that meets the stream — full scale at 105 dB SPL — is SMPTE RP 200's, stated as an assumption in `hz_cluster::scene`. |

Reading a project for understanding does not go in this table; only code,
tables, or constants that were carried over do. Material from sources listed as
forbidden in LEGAL.md §3 must never appear here — if it would, the file has to
be rewritten instead.
