[package]
name = "hz-analysis"
description = "Loudness, true peak and dynamic range analysis for harlettizer"
version.workspace = true
license.workspace = true
edition.workspace = true
rust-version.workspace = true
repository.workspace = true
authors.workspace = true

# Dependency-free on purpose: this is measurement, it runs over whole
# programmes, and it has no business pulling anything in.
[dependencies]
hz-core.workspace = true
